import { sqliteTable, text, integer, primaryKey, index } from "drizzle-orm/sqlite-core";
export const posts = sqliteTable("posts", {id:text("id").primaryKey(),title:text("title").notNull(),excerpt:text("excerpt").notNull().default(""),body:text("body").notNull().default(""),kind:text("kind").notNull(),category:text("category").notNull(),sourceUrl:text("source_url").notNull().default(""),mediaUrl:text("media_url").notNull().default(""),status:text("status").notNull().default("draft"),createdAt:integer("created_at").notNull(),updatedAt:integer("updated_at").notNull()},t=>[index("idx_posts_status_created").on(t.status,t.createdAt)]);
export const members=sqliteTable("members",{id:text("id").primaryKey(),name:text("name").notNull(),createdAt:integer("created_at").notNull()});
export const likes=sqliteTable("likes",{postId:text("post_id").notNull().references(()=>posts.id,{onDelete:"cascade"}),userId:text("user_id").notNull(),createdAt:integer("created_at").notNull()},t=>[primaryKey({columns:[t.postId,t.userId]})]);
export const comments=sqliteTable("comments",{id:text("id").primaryKey(),postId:text("post_id").notNull().references(()=>posts.id,{onDelete:"cascade"}),userId:text("user_id").notNull(),name:text("name").notNull(),body:text("body").notNull(),status:text("status").notNull().default("visible"),createdAt:integer("created_at").notNull()},t=>[index("idx_comments_post_created").on(t.postId,t.createdAt),index("idx_comments_user_created").on(t.userId,t.createdAt)]);
export const visitors=sqliteTable("visitors",{id:text("id").primaryKey(),previewUntil:integer("preview_until").notNull(),createdAt:integer("created_at").notNull()});
export const visits=sqliteTable("visits",{visitorId:text("visitor_id").notNull(),day:text("day").notNull(),createdAt:integer("created_at").notNull()},t=>[primaryKey({columns:[t.visitorId,t.day]}),index("idx_visits_day").on(t.day)]);
export const views=sqliteTable("views",{postId:text("post_id").notNull().references(()=>posts.id,{onDelete:"cascade"}),visitorId:text("visitor_id").notNull(),day:text("day").notNull(),createdAt:integer("created_at").notNull()},t=>[primaryKey({columns:[t.postId,t.visitorId,t.day]}),index("idx_views_day").on(t.day)]);
